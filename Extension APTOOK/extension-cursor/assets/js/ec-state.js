window.ExtensionCursorModules = window.ExtensionCursorModules || {};
window.ExtensionCursorModules.createEcState = function createEcState() {
  const state = {
    loadingKey: false,
  };

  return state;
}
